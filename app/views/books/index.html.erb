<div>
  <h1 class="font-bold text-4xl">Annotated Books</h1>

  <div class="p-4 mt-8 shadow-md">
    <% @books.each do |book| %>
      <%= link_to book.title, book %><br>
      <span class="text-xs"><%= book.author %></span>
    <% end %>
  </div>

</div>
