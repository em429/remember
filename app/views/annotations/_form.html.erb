<%= form_with url: '/import' do |f| %>
  <%= f.label :opf_file, "Import Annotations (.opf)", class:"font-bold" %><br>
  <%= f.file_field :opf_file, class: "mb-4 mt-2" %>

  <%= f.hidden_field :book_id, { value: book.id } %>
  
  <%= f.submit "Import", class: "btn" %>
<% end %>

