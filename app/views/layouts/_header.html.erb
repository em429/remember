<nav class="mb-10 flex justify-between">
     
  <%= link_to 're-mem-ber', root_path,
      class: "px-2 border border-4 border-dashed border-gray-300" %>
      
  <div class="space-x-8 font-bold justify-self-end">
    <%= render_account_dropdown %>
  </div>
  
</nav>

<nav class="flex items-center text-xl justify-between">
  <div class="space-x-8 font-bold">
    <%= nav_link_to "All", annotations_path %>

    <span class="space-x-1">
      <%= nav_link_to "Due", annotations_path(mode: "flashcards_due") %>
      <span class="text-gray-300">
        <span class=""><%= current_user.annotations.filter_by_due.count %></span>
      </span>
    </span>

    <span class="space-x-1">
      <%= nav_link_to "Fresh", annotations_path(mode: "flashcards_fresh") %>
      <span class="text-gray-300">
        <span class=""><%= current_user.annotations.filter_by_fresh.count %></span>
      </span
    </span>

  </div>
  <div class="space-x-8 font-bold">
    <%= nav_link_to "My Books", books_path %>
    <%= link_to "Add Book", new_book_path %>
  </div>
</nav>

