<%= form_with(model: english_word) do |form| %>

  <% if english_word.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium mt-3">
      <h2><%= pluralize(english_word.errors.count, "error") %> prohibited this feed from being saved:</h2>

      <ul>
        <% english_word.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :word %>
    <%= form.text_field :word, class: "block shadow  border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div class="inline">
    <%= form.submit "Update", class: "btn-lg" %>
  </div>
  
<% end %>
